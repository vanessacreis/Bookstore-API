class BookDAO {
  constructor(database) {
    this.database = database;
  }

  allBooks = () => {};

  showOneBook = (name) => {};

  insertBook = (name) => {};

  deleteBook = (name) => {};
}

export default BookDAO;
